# Copy from the backend-blueprint package

```bash
cp -r node_modules/backend-blueprint/prisma/schema/* prisma/schema/
```

Or symlink on Unix:

```bash
ln -s ../node_modules/backend-blueprint/prisma/schema prisma/schema-blueprint
```
